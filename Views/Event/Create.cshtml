@{
    ViewData["Title"] = "Home Page";
}

@section Stylesheets {
    <link rel="stylesheet" href="~/css/create.css" asp-append-version="true" />
}


<div class="filter-section">
    <button type="button" class="filter"><i class="fa-solid fa-sliders"></i></button>
</div>

    <div class="createPost">
        <div class="header"><h2>Create Post</h2></div>
        <hr><br>
        <form class="postForm" method="post" action='@Url.Action("Create", "Event")'>
            <div class="header"><h2>Property Category</h2></div>
            <div class="PropertyCategory">
                <div class="category-boxes">
                @foreach (var categoryKey in HaBuddies.Constants.Category.DataDict.Keys)
                {
                    <div class="category-box" data-category="@categoryKey.ToLower()">
                        @Html.Raw(HaBuddies.Constants.Category.GetIconHtml(categoryKey))
                        <br />
                        @categoryKey
                    </div>
                }
            </div>
            <input type="hidden" class="category" name="Category" required>
            </div>
            <div class="header"><h2>Detail</h2></div>
            <div class="detailbox">
                <div class="inputtitle">
                    <label for="Title">Title</label>
                    <input type="text" class="title" id="Title" name="Title" required>
                </div>
                <div class="inputPeople">
                    <label for="EnrollSize">People</label>
                    <input type="number" class="enrollSize" id="EnrollSize" name="EnrollSize" required>
                </div>
                <div class="inputdate">
                    <label for="EndDate">Closedate</label>
                    <input type="date" class="endDate" id="EndDate" name="EndDate" required>
                </div>
            </div>

                <div class="inputDescription">
                    <label for="description">Description</label>
                    <input type="text"class="description" id="Description" name="Description" required>
                </div>
            
            <div class="header"><h2>Requirement</h2></div>
            <div class="requirement">
                <div class="inputRequirement">
                    <label for="MinAgeRequirement">Minage</label>
                    <input type="number" class="minAge" id="MinAgeRequirement" name="MinAgeRequirement" value= "" >
                </div>
                <div class="inputRequirement">
                    <label for="MaxAgeRequirement">Maxage</label>
                    <input type="number" class="maxAge" id="MaxAgeRequirement" name="MaxAgeRequirement" value="" >
                </div>
                @* <div class="inputRequirement">
                    <label>Gender</label>
                    <div>
                        <input type="checkbox" id="MaleGender" name="GenderRequirement" value="Male" @(Model?.Gender == "Male" ? "checked" : "")>
                        <label for="MaleGender">Male</label>
                    </div>
                    <div>
                        <input type="checkbox" id="FemaleGender" name="GenderRequirement" value="Female" @(Model?.Gender == "Female" ? "checked" : "")>
                        <label for="FemaleGender">Female</label>
                    </div>
                    <div>
                        <input type="checkbox" id="OthersGender" name="GenderRequirement" value="Others" @(Model?.Gender == "Others" ? "checked" : "")>
                        <label for="OthersGender">Others</label>
                    </div>
                    <input type="hidden" name="GenderRequirement" value="null" />
                </div> *@
                <div class="inputRequirement">
                    <label for="GenderRequirement">Gender</label>
                    <select class="gender" id="GenderRequirement" name="GenderRequirement" >
                        <option value="Null"></option>
                        <option value="Male">Male</option>
                        <option value="Female">Female</option>
                        <option value="Others">Others</option>
                    </select>
                </div>

                

            </div>
            <input type="submit" value="Create" >

        </form>
        <script>
            @* document.querySelectorAll('.category-box').forEach(function(box) {
                box.addEventListener('click', function() {
                    document.querySelectorAll('.category-box').forEach(function(box) {
                        box.classList.remove('selected');
                    });
                    this.classList.add('selected');
                    document.querySelector('.category').value = this.getAttribute('data-category');
                });
            });   *@

            @* document.querySelectorAll('.category-box').forEach(function(box) {
                box.addEventListener('click', function() {
                    document.querySelectorAll('.category-box').forEach(function(box) {
                        box.classList.remove('selected');
                        box.style.backgroundColor = ""; 
                        box.style.borderColor = ""; 
                    });
                    this.classList.add('selected');
                    document.querySelector('.category').value = this.getAttribute('data-category');

                    var category = this.getAttribute('data-category').toLowerCase();
                    var colorHex = (HaBuddies.Constants.Category.GetColorHex(category);
                    if (colorHex) {
                        this.style.backgroundColor = colorHex;
                        this.style.borderColor = colorHex; 
                    }
                });
            }); *@



        </script>
    </div>

